import { StyleSheet } from "react-native";

const styles=StyleSheet.create({
img:{
    resizeMode:'contain',
    height:'90%',
    width:'70%',
    alignSelf:'center',
    marginTop:30,
}
});

export default styles;